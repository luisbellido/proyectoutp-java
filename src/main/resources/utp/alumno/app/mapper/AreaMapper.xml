<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="utp.alumno.app.mapper.AreaMapper">
   
    <select id="AllAreas" resultType="area">
    	SELECT a.id, a.name_area as namearea FROM areas a
    </select>

</mapper>